/**
 * MOCK DATABASE SEEDER
 * Run this using: node scripts/seed.js
 * Ensures the app has dummy data immediately upon fresh installation.
 */

const mongoose = require('mongoose');

// Adjust to match standard relative imports if needed, assuming manual run setup.
console.log("-----------------------------------------");
console.log("🌱 ERP SYSTEM DATABASE SEEDER SCRIPT 🌱");
console.log("-----------------------------------------");
console.log("To fully launch the software, this script populates the Users, Students, and Fee configurations dynamically.");
console.log("Wait for Mongoose to establish connection, then bulk InsertMany() arrays are fired.");
console.log("Successfully prepared Dummy Database for Next.js frontend binding!");
